namespace Alliance.Client.Features.Control;

public sealed class ControlProfile
{
    public string ProfileName { get; init; } = "Reserved";

    public string Notes { get; init; } = "Keyboard and mouse mappings will be introduced when command publishing is implemented.";
}
